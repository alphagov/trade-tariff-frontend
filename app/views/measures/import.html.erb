<%= render partial: 'measures/third_country',
    locals: { measures: @import_measures[:third_country_measures] } %>

<%= render partial: 'measures/specific_country',
    locals: { measures: @import_measures[:specific_measures] } %>
