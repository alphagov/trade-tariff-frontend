<article class="tariff commodity">
  <div class="inner">
    <nav class="trail">
      <dl>
        <dt><%= declarable.section.numeral %></dt>
        <dd class="section-heading tariff-fragments"><%= link_to declarable.section, section_path(declarable.section) %>
          <dl class="tariff-fragments">
            <dt><%= declarable.chapter.short_code %></dt>
            <dd><%= link_to declarable.chapter, chapter_path(declarable.chapter) %>
              <dl>
                <dt><%= declarable.heading.display_short_code %></dt>
                <dd><%= link_to declarable.heading, heading_path(declarable.heading) %>
                    <% if with_commodity_tree %>
                      <%= commodity_tree(declarable, declarable.ancestors) %>
                    <% end %>
                </dd>
              </dl>
            </dd>
          </dl>
        </dd>
      </dl>
    </nav>
  </div>
  <%= render 'measures/measures', measurable: declarable %>
</article>
